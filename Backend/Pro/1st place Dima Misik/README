Чтоб собрать сервис необходимо:
    - ОС: Windows
    - запустить docker-compose up
    ! возможно docker-compose up нужно будет выполнить два раза т.к. c# может не приатачится к базе с первого раза

Сервис будет доступен по урле http://localhost:8080
API документация находится по пути http://localhost:8080/swagger. Там описанно доступное API.

CSV формат чуствительный, не редактировать в MS Excel, использовать MS Excel в режиме read-only.
Редактировать CSV только в обычном редакторе.
Пример CSV файла лежит в папке SportChallenge.Examples.

Как аплоадить CSV:
    - Для CSV не нужно добавлять секцию round если добавленая игра находится в другом раунде.
    - Важно только правильно указать порядок home и guest команд.
    - Загружать CSV нужно с помощью form-data, CSV заатачить как файл (есть скрин с постмана в папке SportChallenge.Examples).

Чтоб запустить интеграционные тесты необходимо :
    1. установить .NET Core 2.1 SDK
    2. зайти в папку SportChallenge.ApiTests
    3. открыть testsessings.json
    4. поправить connection string в базу
    5. выполнить команду dotnet test

Чтоб запустить юнит тесты необходимо :
    1. установить .NET Core 2.1 SDK
    2. зайти в папку SportChallenge.UnitTests
    3. выполнить команду dotnet test